import Resource = require('./resource');
import Collection = require('./collection');

class Corporation {
  name: string;
  corporateID: string;
  moneyOut:number;
  influenceOut: number;
  }
  
  export = Corporation;
